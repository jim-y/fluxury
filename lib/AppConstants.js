'use strict';

/**
 * Constants are declared here for the Pirate Badge application
 * @module AppConstants
 */

/**
 * @const
 * @type {string}
 */
const CREATE = 'CREATE';

/**
 * @const
 * @type {string}
 */
const RANDOM = 'RANDOM';

/**
 * @const
 * @type {string}
 */
const CHANGE_EVENT = 'change';

export default { CREATE, RANDOM, CHANGE_EVENT };
